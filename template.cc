#include <iostream>
#include <fstream>

using namespace std;

int main(int argc, char** argv)
{
    if(argc < parametri)
    {
        std::cout << "Usage: ./a <parametro>" << std::endl; 
        return -1;
    }




    return 0;
}